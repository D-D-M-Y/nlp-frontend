import FileSkeleton from "@/app/components/fileSkeleton";
export default function Loading() {
    // Display placeholders while waiting for the actual values to be loaded
    return (
     <FileSkeleton/>
    );
};

